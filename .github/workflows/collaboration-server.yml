name: Collaboration server

on:
  pull_request:
    branches: [devel]
    paths:
      - 'services/collaboration-server/**'
      - '.github/workflows/collaboration-server.yml'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/collaboration-server
    steps:
      - uses: actions/checkout@v5
      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: services/collaboration-server/package-lock.json
      - name: Install dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Test and typecheck
        run: |
          set -o pipefail
          npm test 2>&1 | tee collaboration-server.log
          npm run build 2>&1 | tee -a collaboration-server.log
      - name: Upload diagnostics
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: collaboration-server-${{ github.run_id }}
          path: services/collaboration-server/collaboration-server.log
          retention-days: 1
          if-no-files-found: error
