name: Studio collaboration lock

on:
  pull_request:
    branches: [devel]
    paths:
      - '.github/workflows/studio-collaboration-lock.yml'

permissions:
  contents: write

jobs:
  lock:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.head_ref }}
          show-progress: false
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - name: Update lockfile
        working-directory: rhwp-studio
        run: npm install --package-lock-only --ignore-scripts --no-audit --no-fund > studio-lock.log 2>&1
      - name: Upload lockfile diagnostics
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: studio-lock-${{ github.run_id }}
          path: rhwp-studio/studio-lock.log
          retention-days: 1
          if-no-files-found: error
      - name: Install and verify collaboration tests
        working-directory: rhwp-studio
        run: |
          npm ci --ignore-scripts --no-audit --no-fund
          node --test --import tsx tests/collaboration-*.test.ts
          npm run build
      - name: Commit lockfile
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add rhwp-studio/package-lock.json
          git commit -m "chore: lock studio collaboration dependencies"
          git push origin HEAD:${{ github.head_ref }}
